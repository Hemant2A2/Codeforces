#include<iostream>
#include <vector>
using namespace std;
typedef long long ll;
const ll mod = 1e9 + 7;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int k,n;
    cin >> k >> n;
    vector<int>rest;
    for(int i = 0; i < 26; i++)
    {
        int h;
        cin >> h;
        rest.push_back(h);
    }

    for(int i = 0; i < k; i++)
    {
        
    }
    return 0;
}